% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_analysis.R
\name{add_inverse_variance_weights}
\alias{add_inverse_variance_weights}
\title{Add inverse-variance weights to a tidy effects dataframe}
\usage{
add_inverse_variance_weights(data, group_vars, se_col = "std.error")
}
\arguments{
\item{data}{A dataframe with at least \code{std.error} column.}

\item{group_vars}{Character vector of columns defining groups within which
weights should sum to 1 (e.g., \code{c("outcome", "term")}).}

\item{se_col}{Name of the standard error column. Default \code{"std.error"}.}
}
\value{
\code{data} with an added \code{weight} column, minus any rows with invalid SE.
}
\description{
Computes per-row inverse-variance weights (1 / SE^2), normalized to sum to 1
within each group. Rows with missing, zero, or negative standard errors are
dropped with a warning, since they cannot be validly weighted.
}
