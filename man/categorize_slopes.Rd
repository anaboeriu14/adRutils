% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorize_slopes.R
\name{categorize_slopes}
\alias{categorize_slopes}
\title{Categorize random slopes from a mixed-effects model}
\usage{
categorize_slopes(
  model,
  id_col = "id",
  slope_term = "time",
  methods = c("1sd", "1.5sd", "tertile", "quartile"),
  labels = list(low = "Slow", mid = "Typical", high = "Fast"),
  prefix = "group",
  ref_level = labels$mid
)
}
\arguments{
\item{model}{A fitted \code{lmerMod} object with a random slope term.}

\item{id_col}{Name for the ID column in the output. Default \code{"id"}.}

\item{slope_term}{Name of the random slope variable. Default \code{"time"}.}

\item{methods}{Character vector: any of \code{"1sd"}, \code{"1.5sd"}, \code{"tertile"}, \code{"quartile"}.}

\item{labels}{Named list with \code{"low"}, \code{"mid"}, \code{"high"} group labels.}

\item{prefix}{Prefix for output columns (e.g., \code{group_1sd}). Default \code{"group"}.}

\item{ref_level}{Reference level for the output factor. Default is \code{labels$mid}.}
}
\value{
A tibble with \code{id}, \code{random_intercept}, \code{random_slope}, \code{slope_z},
and one factor column per method.
}
\description{
Extracts random slopes from a fitted \code{lme4} model, z-scores them, and
assigns group labels using SD-based or quantile-based cutoffs.
}
\examples{
\dontrun{
model <- lmer(pace ~ time + (1 + time | id), data = long_data)
trajectories <- categorize_slopes(model, id_col = "subject_id")

# Custom labels
categorize_slopes(
  model,
  labels = list(low = "Decreasing", mid = "Stable", high = "Increasing"),
  prefix = "bmi_traj"
)
}

}
