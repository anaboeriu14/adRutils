% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coalesce_timepoints.R
\name{coalesce_timepoints}
\alias{coalesce_timepoints}
\title{Coalesce measures across longitudinal timepoints}
\usage{
coalesce_timepoints(
  data,
  base_names,
  timepoints,
  final_suffix = "_final",
  source_suffix = "_source",
  timepoint_labels = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{base_names}{Character vector of variable base names (e.g., \code{"dsst"}, \code{"ravlt"}).}

\item{timepoints}{Character vector of suffixes in priority order (first = preferred).}

\item{final_suffix}{Suffix for coalesced column. Default \code{"_final"}.}

\item{source_suffix}{Suffix for source-tracking column. Default \code{"_source"}.}

\item{timepoint_labels}{Optional named vector mapping suffixes to display labels.
If \code{NULL}, suffixes are used as-is.}
}
\value{
The input data with added \verb{*_final} and \verb{*_source} columns.
}
\description{
For each base variable, selects the first non-missing value across timepoints
in priority order. Creates \verb{*_final} and \verb{*_source} columns.
}
\examples{
\dontrun{
data <- coalesce_timepoints(
  data,
  base_names = c("dsst", "ravlt", "moca"),
  timepoints = c("35", "30")
)
}

}
