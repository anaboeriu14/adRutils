% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_timepoints.R
\name{combine_timepoints}
\alias{combine_timepoints}
\title{Combine measures across longitudinal visits}
\usage{
combine_timepoints(
  data,
  base_names,
  timepoints,
  final_suffix = "_final",
  source_suffix = "_source",
  timepoint_labels = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{base_names}{Character vector of variable base names (e.g., \code{"dsst"},
\code{"ravlt"}).}

\item{timepoints}{Character vector of suffixes in priority order; the
first entry is the preferred source.}

\item{final_suffix}{Suffix for the coalesced value column. Default
\code{"_final"}.}

\item{source_suffix}{Suffix for the source-tracking column. Default
\code{"_source"}.}

\item{timepoint_labels}{Optional named character vector mapping suffixes
to display labels. If \code{NULL}, suffixes are used verbatim.}
}
\value{
\code{data} with \verb{<base>_final} and \verb{<base>_source} columns added for
each base name that has at least one matching timepoint column.
}
\description{
For each base variable, selects the first non-missing value across a
priority-ordered sequence of timepoint suffixes. Creates two columns per
base variable: a coalesced value and a source label indicating which
timepoint contributed.
}
\examples{
df <- data.frame(
  dsst30  = c(45, NA, 38),
  dsst35  = c(NA, 52, 41),
  ravlt30 = c(NA, 30, NA),
  ravlt35 = c(28, 32, 35)
)
combine_timepoints(
  df,
  base_names = c("dsst", "ravlt"),
  timepoints = c("30", "35")
)

}
