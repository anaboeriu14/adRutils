% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_args.R
\name{custom_check}
\alias{custom_check}
\title{Build a lazily-evaluated custom validation check}
\usage{
custom_check(condition, message)
}
\arguments{
\item{condition}{An unquoted logical expression (e.g. \code{nrow(data) > 0}).
If evaluation throws an error, the check fails with a message
referring to the original error.}

\item{message}{A cli-flavored error message to report when the
condition is \code{FALSE}. Use \verb{\{.arg name\}}, \verb{\{.val val\}}, etc. as in
\code{\link[cli:cli_abort]{cli::cli_abort()}}.}
}
\value{
A list with a quoted \code{condition} and a \code{message} string, of
class \code{"adRutils_custom_check"}.
}
\description{
Captures \code{condition} as an unevaluated expression so it can be safely
evaluated inside \code{\link[=validate_args]{validate_args()}}, even if it references values that
might be \code{NULL} or fail to evaluate. The expression is evaluated in the
calling function's environment by default.
}
\examples{
\dontrun{
validate_args(
  x = is_string(),
  custom_checks = list(
    custom_check(nrow(data) > 0, "{.arg data} must have rows")
  )
)
}

}
