% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{detect_outlier_thresholds}
\alias{detect_outlier_thresholds}
\title{Detect outlier thresholds via the IQR rule}
\usage{
detect_outlier_thresholds(dataf, var_name, multiplier = 1.5, label = NULL)
}
\arguments{
\item{dataf}{A data frame.}

\item{var_name}{Name of a single numeric column in \code{dataf}.}

\item{multiplier}{IQR multiplier. Default \code{1.5} (Tukey's classic
threshold); use \code{3} for "extreme" outliers.}

\item{label}{Optional label for the \code{notes} column. Defaults to
\code{var_name}.}
}
\value{
A one-row data frame with columns \code{LB} (lower bound), \code{UB}
(upper bound), and \code{notes}.
}
\description{
Computes lower and upper outlier bounds for a numeric variable using
the IQR rule: \code{Q1 - k*IQR} and \code{Q3 + k*IQR}, with \code{k = multiplier}.
}
\examples{
df <- data.frame(x = c(1:10, 100))
detect_outlier_thresholds(df, "x")
detect_outlier_thresholds(df, "x", multiplier = 3)

}
