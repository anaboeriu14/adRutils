% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing_values.R
\name{drop_cols_by_missingness}
\alias{drop_cols_by_missingness}
\title{Drop columns with a high percentage of missing values}
\usage{
drop_cols_by_missingness(
  data,
  threshold = 99,
  na_strings = NULL,
  quiet = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{threshold}{Numeric in \verb{[0, 100]}. Columns at or above this
missingness percentage are removed. Default \code{99}.}

\item{na_strings}{Optional character vector of string values to treat as
missing, compared after trimming whitespace (so \code{""} also matches
whitespace-only entries). Applied to character and factor columns only.
When \code{NULL} (default), only true \code{NA} is counted.}

\item{quiet}{If \code{TRUE}, suppress messages. Default \code{FALSE}.}
}
\value{
\code{data} with high-missingness columns removed.
}
\description{
Removes columns from \code{data} whose missingness percentage is at or above
\code{threshold}. To inspect what would be dropped without modifying the data,
call \code{\link[=summarize_missingness]{summarize_missingness()}} with the same \code{threshold} (and \code{na_strings}).
}
\examples{
df <- data.frame(
  id               = 1:100,
  almost_empty     = c(rep(NA, 99), 1),
  completely_empty = rep(NA, 100),
  mostly_filled    = c(rep(NA, 20), 1:80)
)
drop_cols_by_missingness(df, threshold = 90)

}
\seealso{
\code{\link[=summarize_missingness]{summarize_missingness()}} for inspecting missingness without
removing columns.
}
