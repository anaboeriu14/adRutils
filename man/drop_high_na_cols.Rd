% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing_values.R
\name{drop_high_na_cols}
\alias{drop_high_na_cols}
\title{Drop columns with a high percentage of NA values}
\usage{
drop_high_na_cols(data, threshold = 99, quiet = FALSE)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{threshold}{Numeric in \verb{[0, 100]}. Columns with
\code{percent_na >= threshold} are removed. Default \code{99}.}

\item{quiet}{If \code{TRUE}, suppress messages. Default \code{FALSE}.}
}
\value{
\code{data} with high-NA columns removed.
}
\description{
Removes columns from \code{data} whose \code{NA} percentage is at or above
\code{threshold}. To inspect what would be dropped without modifying the
data, call \code{\link[=summarize_na]{summarize_na()}} with the same threshold.
}
\examples{
df <- data.frame(
  id              = 1:100,
  almost_empty    = c(rep(NA, 99), 1),
  completely_empty = rep(NA, 100),
  mostly_filled   = c(rep(NA, 20), 1:80)
)
drop_high_na_cols(df, threshold = 90)

}
\seealso{
\code{\link[=summarize_na]{summarize_na()}} for inspecting NA patterns without removing
columns.
}
