% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_coefficients.R
\name{extract_coefficients}
\alias{extract_coefficients}
\title{Extract raw coefficients with confidence intervals}
\usage{
extract_coefficients(model_results, term_pattern = NULL, ci_level = 0.95)
}
\arguments{
\item{model_results}{A tibble containing a list-column \code{model_res} of tidy
model results (as produced by \code{\link[=fit_models_by_group]{fit_models_by_group()}}).}

\item{term_pattern}{Optional regex to filter \code{term} values.}

\item{ci_level}{Confidence level for the intervals. Default \code{0.95}.}
}
\value{
A tibble with one row per coefficient, including columns
\code{estimate}, \code{std.error}, \code{conf.low}, \code{conf.high}, \code{p.value}, and \code{sig}
(TRUE when the CI excludes zero).
}
\description{
Extracts coefficients from a tibble of fitted models (typically the output
of \code{\link[=fit_models_by_group]{fit_models_by_group()}}) and adds confidence intervals computed from
the model standard errors.
}
\examples{
\dontrun{
results <- fit_models_by_group(data, outcomes, predictors)
extract_coefficients(results, term_pattern = "treatment")
}

}
