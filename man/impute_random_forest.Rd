% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_random_forest.R
\name{impute_random_forest}
\alias{impute_random_forest}
\title{Impute missing data using random forest}
\usage{
impute_random_forest(
  dataf,
  vars_to_impute,
  helper_vars,
  parallel = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dataf}{A data frame.}

\item{vars_to_impute}{Character vector of columns to impute.}

\item{helper_vars}{Character vector of additional columns used as
predictors during imputation. Must not overlap \code{vars_to_impute}.}

\item{parallel}{If \code{TRUE}, use the \code{"forests"} parallelization mode of
\code{\link[missForest:missForest]{missForest::missForest()}}. Default \code{FALSE}. Requires registering a
parallel backend (e.g., \code{\link[doParallel:registerDoParallel]{doParallel::registerDoParallel()}}) \emph{before}
calling.}

\item{quiet}{If \code{TRUE}, suppress messages. Default \code{FALSE}.}
}
\value{
\code{dataf} with imputed columns appended (named \verb{i_<var>}). The
per-variable out-of-bag imputation error is attached as an attribute
named \code{"imputation_errors"} (a tibble with \code{variable} and \code{MSE}
columns); access it via \code{attr(result, "imputation_errors")}.
}
\description{
Imputes missing values in \code{vars_to_impute} using
\code{\link[missForest:missForest]{missForest::missForest()}}, a non-parametric Random Forest imputation
method. Helper variables contribute to the imputation model but are
not themselves imputed in the output.
}
\details{
Imputed columns are added with an \code{i_} prefix; original columns are
preserved unchanged.

For error estimation, \code{\link[missForest:missForest]{missForest::missForest()}} is given the complete
cases of the input as \code{xtrue}. When few complete cases are available,
the resulting error estimates may be unreliable; a warning is issued
if fewer than 10\% of rows are complete.
}
\examples{
\dontrun{
# Optional: register a parallel backend before calling with parallel = TRUE
doParallel::registerDoParallel(cores = 3)

result <- impute_random_forest(
  dataf          = mydata,
  vars_to_impute = c("BMI", "glucose"),
  helper_vars    = c("age", "sex", "weight"),
  parallel       = TRUE
)

summary(result$i_BMI)
attr(result, "imputation_errors")
}

}
