% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_args.R
\name{new_assertion}
\alias{new_assertion}
\title{Create a new validation assertion}
\usage{
new_assertion(check_fn)
}
\arguments{
\item{check_fn}{A function \verb{function(x, nm)} that returns \code{NULL} on
success or a cli-flavored error message string on failure. \code{x} is
the value being checked; \code{nm} is the argument name (used in the
error message).}
}
\value{
An object of class \code{"adRutils_assertion"}.
}
\description{
Low-level constructor for building custom assertion helpers. Most users
should use the built-in helpers (\code{\link[=is_flag]{is_flag()}}, \code{\link[=is_string]{is_string()}}, etc.) or
\code{\link[=custom_check]{custom_check()}} for one-off checks. Use \code{new_assertion()} only when
building a \emph{reusable} assertion that you want to call from multiple
functions.
}
\examples{
is_probability <- function() {
  new_assertion(function(x, nm) {
    if (!is.numeric(x) || length(x) != 1L || is.na(x) || x < 0 || x > 1) {
      sprintf("{.arg {nm}} must be a single number in [0, 1], not \%s",
              format(x))
    }
  })
}

}
