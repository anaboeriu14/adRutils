% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_duplicates.R
\name{remove_duplicates}
\alias{remove_duplicates}
\title{Remove duplicate rows by ID}
\usage{
remove_duplicates(
  dataf,
  id_col,
  keep = c("most_complete", "first", "last", "none"),
  quiet = FALSE
)
}
\arguments{
\item{dataf}{A data frame.}

\item{id_col}{Column name (quoted or unquoted) identifying rows.}

\item{keep}{One of \code{"most_complete"} (default), \code{"first"}, \code{"last"}, or
\code{"none"}. \code{"none"} removes \emph{all} rows with duplicate IDs, including
their originals.}

\item{quiet}{If \code{TRUE}, suppress messages. Default \code{FALSE}.}
}
\value{
\code{dataf} with duplicates removed according to \code{keep}.
}
\description{
Identifies and removes duplicate rows based on a single ID column. By
default, keeps the row with the fewest \code{NA} values per duplicate ID
(the "most complete" row).
}
\examples{
df <- data.frame(
  id  = c(1, 2, 2, 3),
  val = c(10, NA, 20, 30)
)
remove_duplicates(df, id_col = "id")  # keeps id=2 row with val=20

}
